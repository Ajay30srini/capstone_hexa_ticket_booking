## 🚀 Run with Docker

### 1. Clone repo
git clone <repo_url>
cd ticket-booking-api

### 2. Create .env.docker
Copy .env.example → .env.docker

### 3. Start containers
docker compose up --build

### 4. Open Swagger
http://localhost:8000/docs